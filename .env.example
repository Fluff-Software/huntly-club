# Supabase — local: use ngrok URL + anon key from `supabase status`; hosted: use project URL + anon key from Supabase dashboard → Settings → API
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Admin app (server-only): same URL; use service_role key from Supabase dashboard → Settings → API. Never expose in client.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# EAS (from Expo dashboard)
EXPO_PUBLIC_EAS_PROJECT_ID=
EXPO_PUBLIC_OWNER=

# RevenueCat (optional for builds)
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=
