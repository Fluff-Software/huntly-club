/**
 * App uses a single light theme for consistency across Android and iOS (and web).
 * Returns "light" always; dark mode is not used.
 */
export function useColorScheme(): "light" {
  return "light";
}
