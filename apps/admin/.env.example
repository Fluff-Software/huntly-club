# Admin app auth (browser + middleware)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side data and admins check (never expose in client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Compass AI (server-side only, never expose in client)
OPENROUTER_API_KEY=
COMPASS_DAILY_COST_CEILING_USD=5